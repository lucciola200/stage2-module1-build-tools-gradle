//add everything here
group = 'com.example'
version = '0.0.1'

repositories {
    mavenCentral()
}

dependencies {
    implementation project(':utils')
}

test {
    useJUnitPlatform()
}

compileJava {}.dependsOn(':utils:publishToMavenLocal')
